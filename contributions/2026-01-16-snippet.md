# Code Snippet: Debounce Function

**Date:** January 16, 2026

## Description

A type-safe debounce function that delays execution until after a specified wait time has elapsed since the last call. Useful for search inputs and resize handlers.

## Code

```typescript
function debounce<T extends (...args: any[]) => any>(
  func: T,
  wait: number
): (...args: Parameters<T>) => void {
  let timeout: NodeJS.Timeout | null = null;
  
  return function executedFunction(...args: Parameters<T>): void {
    const later = () => {
      timeout = null;
      func(...args);
    };
    
    if (timeout) clearTimeout(timeout);
    timeout = setTimeout(later, wait);
  };
}

// Usage
const handleSearch = debounce((query: string) => {
  console.log('Searching for:', query);
}, 300);
```

## Use Cases

- Production-ready utility function
- Reusable across projects
- Well-tested pattern
- Type-safe implementation

---
*Part of my code snippet collection for quick reference and reuse.*
