# Code Snippet: Retry with Exponential Backoff

**Date:** January 27, 2026

## Description

Retries an async function with exponential backoff, useful for API calls and network requests with proper TypeScript typing.

## Code

```typescript
async function retryWithBackoff<T>(
  fn: () => Promise<T>,
  maxRetries: number = 3,
  baseDelay: number = 1000
): Promise<T> {
  for (let i = 0; i < maxRetries; i++) {
    try {
      return await fn();
    } catch (error) {
      if (i === maxRetries - 1) throw error;
      
      const delay = baseDelay * Math.pow(2, i);
      console.log(`Retry ${i + 1} after ${delay}ms`);
      await new Promise(resolve => setTimeout(resolve, delay));
    }
  }
  throw new Error('Max retries exceeded');
}
```

## Use Cases

- Production-ready utility function
- Reusable across projects
- Well-tested pattern
- Type-safe implementation

---
*Part of my code snippet collection for quick reference and reuse.*
